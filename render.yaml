# Render Blueprint configuration
services:
  - type: web
    name: vector-deploy-simple-app-the-app-should-simply-say-d1eadd3b
    env: node
    plan: starter
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
